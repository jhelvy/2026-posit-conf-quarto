---
title: "When does it weave?"
subtitle: "Entanglement figure — fresh draft"
format:
  revealjs:
    theme: [default, ../../custom.scss]
    transition: fade
    width: 1280
    height: 720
    slide-number: false
---

<!--
  FRESH DRAFT of the entanglement figure. Replaces the nested-box design.
  The old files (entanglement-anime.qmd, entanglement-diagram.qmd) are untouched.

  DESIGN CONTRACT
  - Two stacked rows, IDENTICAL output bar at the end of each (one <use> of one
    <defs> group, so they cannot drift apart).
  - The output is always woven. Content+format are never drawn as separate boxes
    and never as a box-inside-a-box (containment is the visual opposite of
    entanglement). They are interleaved stripes.
  - The difference between the rows is NOT what comes out. It is:
      (a) WHEN the weaving happens, and
      (b) whether the content has an ADDRESS you can trace back to.
  - The empty gap in row 1 between agent and output is deliberate: it is exactly
    the space that Quarto fills in row 2.
  - amber = content, blue = format/structure, green = the human's direct action.

  BUILD: base + 2 fragments = 3 states, 2 clicks.
    state 1  row 1 (the status quo) + "where is this sentence?"
    state 2  row 2 appears: agent writes only the .qmd, data is separate, render
    state 3  the amber provenance thread + green edit arrows + the payoff line

  Pure SVG + reveal fragments. No anime.js, no CDN, nothing to vendor before
  the talk. Fragments toggle group opacity, which is all this needs.
-->

## When does it weave?

```{=html}
<svg viewBox="0 0 1240 690" width="100%" style="max-height:78vh;"
     xmlns="http://www.w3.org/2000/svg"
     font-family="'Fira Sans Condensed', Tahoma, sans-serif">

  <defs>
    <!-- THE DOCUMENT. Defined once, used twice, so the two rows are provably
         identical. Amber = content, blue = format, interleaved and irregular. -->
    <g id="doc">
      <rect x="0"   y="0" width="18" height="64" fill="#007bff"/>
      <rect x="18"  y="0" width="26" height="64" fill="#C0820F"/>
      <rect x="44"  y="0" width="14" height="64" fill="#007bff"/>
      <rect x="58"  y="0" width="32" height="64" fill="#C0820F"/>
      <rect x="90"  y="0" width="20" height="64" fill="#007bff"/>
      <rect x="110" y="0" width="16" height="64" fill="#C0820F"/>
      <rect x="126" y="0" width="28" height="64" fill="#007bff"/>
      <rect x="154" y="0" width="24" height="64" fill="#C0820F"/>
      <rect x="178" y="0" width="12" height="64" fill="#007bff"/>
      <rect x="190" y="0" width="28" height="64" fill="#C0820F"/>
      <rect x="218" y="0" width="22" height="64" fill="#007bff"/>
      <rect x="0"   y="0" width="240" height="64" fill="none"
            stroke="#121212" stroke-width="2.5"/>
    </g>

    <marker id="ahDark" markerWidth="9" markerHeight="9" refX="7" refY="3"
            orient="auto"><path d="M0,0 L7,3 L0,6 z" fill="#121212"/></marker>
    <marker id="ahGray" markerWidth="9" markerHeight="9" refX="7" refY="3"
            orient="auto"><path d="M0,0 L7,3 L0,6 z" fill="#696969"/></marker>
    <marker id="ahGreen" markerWidth="9" markerHeight="9" refX="7" refY="3"
            orient="auto"><path d="M0,0 L7,3 L0,6 z" fill="#38B44A"/></marker>
    <marker id="ahAmber" markerWidth="9" markerHeight="9" refX="7" refY="3"
            orient="auto"><path d="M0,0 L7,3 L0,6 z" fill="#C0820F"/></marker>
  </defs>

  <!-- ================= ROW 1 — ask for the document ================= -->
  <g>
    <text x="20" y="42" font-size="19" fill="#696969"
          letter-spacing="1.5">ASK FOR THE DOCUMENT</text>

    <!-- where is this sentence? -->
    <text x="894" y="26" font-size="17" fill="#696969"
          text-anchor="middle">where is this sentence?</text>
    <text x="894" y="56" font-size="26" font-weight="700" fill="#E74A2F"
          text-anchor="middle">?</text>
    <path d="M 894 62 L 894 76" stroke="#E74A2F" stroke-width="2"
          stroke-dasharray="3 3"/>

    <!-- me -->
    <rect x="20" y="78" width="100" height="64" rx="6"
          fill="#FFFFFF" stroke="#121212" stroke-width="2.5"/>
    <text x="70" y="117" font-size="22" text-anchor="middle" fill="#121212">me</text>

    <path d="M 122 110 L 174 110" stroke="#121212" stroke-width="2.5"
          marker-end="url(#ahDark)"/>

    <!-- agent -->
    <rect x="180" y="78" width="160" height="64" rx="6"
          fill="#FFFFFF" stroke="#121212" stroke-width="2.5"/>
    <text x="260" y="117" font-size="22" text-anchor="middle" fill="#121212">LLM agent</text>

    <!-- the one big jump. the emptiness here is the point. -->
    <text x="580" y="96" font-size="20" fill="#121212"
          text-anchor="middle">one probabilistic pass</text>
    <path d="M 344 110 L 812 110" stroke="#121212" stroke-width="6"
          marker-end="url(#ahDark)"/>
    <text x="580" y="136" font-size="19" fill="#E74A2F"
          text-anchor="middle">woven here</text>

    <use href="#doc" x="820" y="78"/>

    <!-- the only lever -->
    <path d="M 840 146 L 840 192 Q 840 206 826 206 L 84 206 Q 70 206 70 192 L 70 150"
          fill="none" stroke="#696969" stroke-width="2.5"
          stroke-dasharray="7 5" marker-end="url(#ahGray)"/>
    <text x="455" y="230" font-size="19" fill="#696969"
          text-anchor="middle">review &amp; re-prompt — your only lever</text>
  </g>

  <line x1="20" y1="262" x2="1220" y2="262"
        stroke="#121212" stroke-width="1" opacity="0.15"/>

  <!-- ================= ROW 2 — ask for the source ================= -->
  <g class="fragment" data-fragment-index="1">
    <text x="20" y="304" font-size="19" fill="#696969"
          letter-spacing="1.5">ASK FOR THE SOURCE</text>

    <!-- me -->
    <rect x="20" y="328" width="100" height="64" rx="6"
          fill="#FFFFFF" stroke="#121212" stroke-width="2.5"/>
    <text x="70" y="367" font-size="22" text-anchor="middle" fill="#121212">me</text>

    <path d="M 122 360 L 174 360" stroke="#121212" stroke-width="2.5"
          marker-end="url(#ahDark)"/>

    <!-- agent -->
    <rect x="180" y="328" width="160" height="64" rx="6"
          fill="#FFFFFF" stroke="#121212" stroke-width="2.5"/>
    <text x="260" y="367" font-size="22" text-anchor="middle" fill="#121212">LLM agent</text>

    <text x="387" y="348" font-size="18" fill="#121212"
          text-anchor="middle">writes</text>
    <path d="M 344 360 L 424 360" stroke="#121212" stroke-width="2.5"
          marker-end="url(#ahDark)"/>

    <!-- .qmd = structure -->
    <rect x="430" y="328" width="140" height="64" rx="6"
          fill="#FFFFFF" stroke="#007bff" stroke-width="3.5"/>
    <text x="500" y="367" font-size="22" text-anchor="middle"
          fill="#007bff" font-family="SFMono-Regular, Menlo, monospace">.qmd</text>

    <!-- data = content, and the agent never touches it -->
    <rect x="430" y="470" width="140" height="64" rx="6"
          fill="#FFFFFF" stroke="#C0820F" stroke-width="3.5"/>
    <text x="500" y="509" font-size="22" text-anchor="middle"
          fill="#C0820F" font-family="SFMono-Regular, Menlo, monospace">data</text>

    <!-- both converge into render -->
    <path d="M 574 360 L 648 360" stroke="#007bff" stroke-width="2.5"/>
    <path d="M 574 496 C 626 496, 648 430, 652 372"
          fill="none" stroke="#C0820F" stroke-width="2.5"/>

    <text x="736" y="346" font-size="20" fill="#121212"
          text-anchor="middle">render</text>
    <path d="M 652 360 L 812 360" stroke="#121212" stroke-width="6"
          marker-end="url(#ahDark)"/>
    <text x="736" y="392" font-size="19" fill="#2c8475"
          text-anchor="middle">woven here</text>

    <!-- the same document. deliberately identical. -->
    <use href="#doc" x="820" y="328"/>
  </g>

  <!-- ============ payoff: the address, and reaching in ============ -->
  <g class="fragment" data-fragment-index="2">
    <!-- the traced sentence, highlighted in the output -->
    <rect x="878" y="328" width="32" height="64" fill="none"
          stroke="#C0820F" stroke-width="4"/>
    <text x="894" y="316" font-size="17" fill="#C0820F"
          text-anchor="middle">this one</text>

    <!-- provenance: it came from a row in a file -->
    <path d="M 576 516 C 712 546, 862 512, 890 400"
          fill="none" stroke="#C0820F" stroke-width="3"
          marker-end="url(#ahAmber)"/>
    <text x="742" y="566" font-size="19" fill="#C0820F"
          text-anchor="middle">row 3 of sentences.csv</text>

    <!-- you reach past the agent, into both -->
    <path d="M 70 398 C 70 428, 96 432, 136 432 L 380 432 C 410 432, 420 412, 426 382"
          fill="none" stroke="#38B44A" stroke-width="3"
          marker-end="url(#ahGreen)"/>
    <path d="M 70 398 C 70 466, 118 502, 196 502 L 420 502"
          fill="none" stroke="#38B44A" stroke-width="3"
          marker-end="url(#ahGreen)"/>
    <text x="248" y="470" font-size="19" fill="#38B44A"
          text-anchor="middle">you edit these directly</text>

    <!-- the line -->
    <text x="620" y="646" font-size="24" fill="#121212" text-anchor="middle">
      Same output. The difference is <tspan font-weight="700">when</tspan> it gets woven —
    </text>
    <text x="620" y="678" font-size="24" fill="#121212" text-anchor="middle">
      and whether you can reach in before it does.
    </text>
  </g>

</svg>
```

::: {.notes}
Both of these weave content and format together. The only question is when — and
by what.

Click 1: the status quo. One pass, everything fused at generation time. Point at
the amber stripe: where did that sentence come from? Nowhere you can name. Your
only lever is asking again.

Click 2: same prompt, same agent — but now it writes the `.qmd`. The data sits
beside it. Render puts them together. Note the output is *identical*. I have not
made a better document.

Click 3: what I've actually bought. That sentence has an address — row 3 of a
CSV. And I can reach past the agent and edit either side myself.

Optional, for this room: "weave" isn't my metaphor. It's literate programming's
own word — Sweave, then knitr. Quarto has always been a weaving tool. I'm just
arguing we should weave later.
:::

## Final state {visibility="uncounted"}

```{=html}
<p style="font-size:0.6em;color:#696969;margin-top:2em;">
Same figure, all groups visible — for export / screenshot. Duplicate the SVG
above with the <code>class="fragment"</code> attributes removed.
</p>
```
