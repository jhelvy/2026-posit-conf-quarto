# The Unreasonable Effectiveness of Quarto

Slides for a 20-minute talk at posit::conf 2026.

Quarto as an abstraction layer that separates *content* from *format* — and why
that matters for working with AI agents.

- **Deck:** `index.qmd` (Quarto RevealJS) — render with `quarto render index.qmd`
- **Theme:** `custom.scss`
- **Abstract:** `abstract.md`

The key entanglement figure is in `images/entanglement-diagram/`.
